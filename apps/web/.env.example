# Storage Configuration
# Options: memory (default), json, postgres

# For JSON file storage (recommended for Vercel with Vercel Blob)
STORAGE_BACKEND=json
STORAGE_FILE_PATH=./data/storage.json

# For PostgreSQL (set up your database first)
# STORAGE_BACKEND=postgres
# DATABASE_URL=postgresql://user:password@localhost:5432/contigo