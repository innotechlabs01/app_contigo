---
name: security
description: Use when handling sensitive data — SecureStorage, certificate pinning, env vars, token encryption, no secrets in code.
---

# Security
SecureStorage, certificate pinning, env vars, token encryption, no secrets in code.
