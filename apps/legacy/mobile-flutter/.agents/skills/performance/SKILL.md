---
name: performance
description: Use when optimizing performance — Const widgets, lazy loading, pagination, image cache, optimized rebuilds, isolates where needed.
---

# Performance
Const widgets, lazy loading, pagination, image cache, optimized rebuilds, isolates where needed.
