version: "3.8"

services:
  api:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    ports:
      - "8082:8080"
    env_file:
      - ../../.env
    environment:
      - LOG_LEVEL=debug
      - LOG_FORMAT=console
    restart: unless-stopped
